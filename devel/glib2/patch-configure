$NetBSD$

--- configure.orig	2014-06-02 05:16:33.116654080 +0000
+++ configure
@@ -30859,6 +30859,10 @@ case $host in
   *-*-beos*)
     glib_os="#define G_OS_BEOS"
     ;;
+  *-*-haiku*)
+    glib_os="#define G_PLATFORM_HAIKU
+#define G_OS_UNIX"
+    ;;
   *-*-cygwin*)
     glib_os="#define G_OS_UNIX
 #define G_PLATFORM_WIN32
